syntax = "proto3";

package swap;

message Swaps {
    repeated Swap swaps = 1;
}

message Swap {
    string signer = 1;
    string tokenIn = 2;
    string tokenOut = 3;
    uint64 amountIn = 4;
    uint64 amountOut = 5;
    string signature = 6;
    string amm = 7;
}
